<%= simple_form_for @obsequio, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :descripcion %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                obsequios_path, :class => 'btn' %>
  </div>
<% end %>
