<%- model_class = Telefono -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:propietario_id_id) %></th>
      <th><%= model_class.human_attribute_name(:numero_telefonico) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @telefonos.each do |telefono| %>
      <tr>
        <td><%= link_to telefono.id, telefono_path(telefono) %></td>
        <td><%= telefono.propietario_id_id %></td>
        <td><%= telefono.numero_telefonico %></td>
        <td><%=l telefono.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_telefono_path(telefono), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      telefono_path(telefono),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_telefono_path,
            :class => 'btn btn-primary' %>
